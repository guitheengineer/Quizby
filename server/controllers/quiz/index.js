const currentQuiz = require('./current-quiz');
const getQuizzes = require('./get-quizzes');
const searchQuizzes = require('./search-quizzes');
const getRecommendedQuiz = require('./get-recommended-quiz');
const categoryQuiz = require('./category-quiz');

module.exports = {
  currentQuiz,
  getQuizzes,
  searchQuizzes,
  getRecommendedQuiz,
  categoryQuiz,
};
